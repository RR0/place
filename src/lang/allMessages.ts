import { PlaceMessages } from "./PlaceMessages"
import { placeMessages_fr } from "./PlaceMessages_fr"
import { placeMessages_en } from "./PlaceMessages_en"

export const allMessages: { [lang: string]: PlaceMessages } = {
  "fr": placeMessages_fr,
  "en": placeMessages_en
}
